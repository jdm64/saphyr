
void foo()
{
	null->as;
	@void->as(int);
	(int)->as(float);
}

void bar()
{
	@(@void,int)int64 a;
	@()int b;

	a = b;
}

========

negative/Cast.syp:4: as operator requires type argument
negative/Cast.syp:5: as operator only operates on expression
negative/Cast.syp:6: as operator only operates on expression
negative/Cast.syp:14: can not cast @()int32 to @(@void,int32)int64
found 4 errors
