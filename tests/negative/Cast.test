
// nofmt

void foo()
{
	null->as;
	@void->as(int);
	(int)->as(float);
}

void bar()
{
	@(@void,int)int64 a;
	@()int b;

	a = b;
}

========

negative/Cast.syp:6: as operator requires type argument
negative/Cast.syp:7: as operator only operates on expression
negative/Cast.syp:8: as operator only operates on expression
negative/Cast.syp:16: Cannot cast type to pointer ( @()int32 to @(@void,int32)int64 )
found 4 errors
