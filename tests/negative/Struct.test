
struct Bad
{
	auto z;
	int j = 9;
	int8 j;
}

struct Good
{
	int v;
	int p;
}

struct Good { int a; }

struct More { bool b; float f; }

void func()
{
	Nothing h;
	Good k;
	int w;

	int q = w.a + 5 + k.n;
}

void func2()
{
	Good x, y;
	More w;

	auto z = x + y;
	z = x + w;
	k.e++;
}

void func3()
{
	Good x;
	More w;

	auto c = x < w;
}

========

negative/Struct.syp:4: struct members must not have auto type
negative/Struct.syp:5: structs don't support variable initialization
negative/Struct.syp:6: member name j already declared
negative/Struct.syp:15: type with name Good already declared
negative/Struct.syp:21: Nothing type not declared
negative/Struct.syp:25: int32 is not a struct/union/enum
negative/Struct.syp:25: Good doesn't have member n
negative/Struct.syp:33: can not perform operation on composite types
negative/Struct.syp:34: can not cast between Good and More types
negative/Struct.syp:35: variable k not declared
negative/Struct.syp:43: can not cast between Good and More types
found 11 errors
