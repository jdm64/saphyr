
enum Color {Red, Green, Blue}

enum Dir {N, S, E, W}

enum Broken { A = 3 + Dir.N, B = 3.4, A = 2 }

int main()
{
	auto a = Color.Red, b = Dir.S;

	auto c = a + b;

	auto d = c.N;

	Dir ee = Color.Red;

	return Color.NotFound;
}

========

Enum.syp: enum initializer must be a constant
negative/Enum.syp:6: enum initializer must be an int-like constant
negative/Enum.syp:6: enum member name A already declared
Enum.syp: c is not a struct/union/enum
Enum.syp: can't cast to enum type
negative/Enum.syp:18: Color doesn't have member NotFound
found 6 errors
