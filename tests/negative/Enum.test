
enum Color {Red, Green, Blue}

enum Dir {N, S, E, W}

enum Broken { A = 3 + Dir.N, B = 3.4, A = 2 }

enum BadBase<[3]int> { XX, YY }

int main()
{
	auto a = Color.Red, b = Dir.S;

	auto c = a + b;

	auto d = c.N;

	Dir ee = Color.Red;

	return Color.NotFound;
}

========

negative/Enum.syp:6: enum initializer must be a constant
negative/Enum.syp:6: enum initializer must be an int-like constant
negative/Enum.syp:6: enum member name A already declared
negative/Enum.syp:8: enum base type must be an integer type
negative/Enum.syp:16: int32 is not a struct/union/enum
negative/Enum.syp:18: can not cast Color to Dir
negative/Enum.syp:20: Color doesn't have member NotFound
found 7 errors
